//First answer

let numbers = [-5, 2, -3, 8, -1, 4];

let nonNegative = numbers.filter(function(number) {
    return number >= 0;
    numbers.map(function(number) {
        return number * number;
      });
    
  });

console.log(nonNegative);